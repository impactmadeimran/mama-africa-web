# Base URL for browser API calls.
# Dev & production: use /api (proxied to the backend — see VITE_API_PROXY_TARGET).
VITE_API_URL=/api

# Backend origin for the /api proxy (Vite dev server + Cloudflare Worker middleware).
VITE_API_PROXY_TARGET=http://localhost:3001
